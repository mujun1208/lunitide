# 最终门禁、候选构建与文档交叉复核

日期：2026-09-06。范围限定为 `release/Verify-Engineering.ps1`、`implementation/finalize-phase2.py`、`release/Build-Release.ps1` 及其现有安全/布局校验依赖；同时核对 PRD、40 项实施台账、56 项测试计划、24 行支持矩阵的交付口径。本次未启动发布构建、未执行安装或签名、未修改发布脚本。

## 1. 结论与实际构建参数

未发现阻止当前工程门禁运行或完整未签名候选构建的脚本缺陷。发现一项必须在本次调用中明确处理的环境条件：本机已配置 `LUNITIDE_SIGN_COMMAND` 和 `LUNITIDE_SIGNER_THUMBPRINT`（仅检查是否存在，未记录其值）。`Build-Release.ps1:27` 的签名函数只判断签名命令是否非空；`-AllowUnsignedDevelopment` 仅取消强制签名要求，并不会跳过已有签名命令。

在所有源码稳定、六组门禁完成并核对后，未签名演练应在 PowerShell 内直接调用：

```powershell
& ./release/Build-Release.ps1 -OutputRoot release/out-final -AllowUnsignedDevelopment -SignCommand '' -ExpectedSignerThumbprint ''
```

本次复核时 `release/out` 已有 3 个条目，会按脚本的不可变候选保护拒绝复用；`release/out-final` 尚不存在。执行前仍应重新确认目标为空。不要传 `-SkipInstaller`，否则不会验证完整 NSIS 安装包构建。该候选属于开发演练，不能因此标记发布者签名、干净安装/更新/卸载或正式发布已经验收。

## 2. 已核对的机制与依赖

- 工程脚本逐项记录真实退出码、stdout/stderr 和起止时间；六组分别覆盖 Go/覆盖率、全库 race、前端契约/类型/测试/构建、Go 静态检查、依赖漏洞、双 PowerShell 发布工具与排除清单。覆盖率下限至少 51%，不是从通过率推算覆盖率。
- 汇总脚本验证六份 passed 回执、同一 commit/源码摘要、准确检查项集合、原始日志存在、当前源文件集合及逐文件 SHA；Go 结果区分顶层测试、子测试、包和 skip，前端核对真实 JSON 结果；不会自动赋分或推断现场验收成功。
- 工程源码摘要有意排除 `docs/`，允许收集证据和整理报告；候选构建摘要包含文档，构建期间也必须冻结文档。构建后添加交付说明时，不能把随后变化的文档误称为候选内原始快照内容。
- Go、npm.cmd、npx.cmd、Node、Python、Windows PowerShell 5.1、PowerShell 7、golangci-lint、govulncheck、GCC 均可找到。已读取三个缓存依赖的实际 SHA，均与构建脚本固定值一致。缓存的 NuGet 验证器实际执行 WebView2 包签名检查返回 exit 0，作者签名及仓库副签名/时间戳验证通过。
- WebView2 包 SHA：`5ea526bbd728adda0da4d31219267e96460494a427e4894c4e09d9f320f4b9aa`；NSIS 包 SHA：`c7d27f780ddb6cffb4730138cd1591e841f4b7edb155856901cdf5f214394fa1`；NuGet 验证器 SHA：`0790bb7a0c898e44b70f2b65e3070b4db8af23897e38b8653d72d268b6e8bb11`。
- 四个产品程序与布局清单一致。解析 worker 使用 engine 的提前分派模式，图形 worker 使用 desktop 的独立进程模式；无需额外未列入安装包的第五个 exe。真正候选构建和最终布局/摘要检验仍由主代理在冻结后执行。

## 3. 主文档最终需更新的具体项

CSV 实际计数为 40 个实施项、56 个测试项、24 行支持范围；ID 无重复，现有证据字段引用的文件可找到。PRD 1.4 已区分整改前 2.7、第一阶段历史暂评 3.6、第二阶段工程评分及真实环境评分；历史基线表和 `*-before-*.csv` 应保持原值。

| 文件/项 | 当前待同步内容 | 有效更新依据 |
| --- | --- | --- |
| `test-plan.csv` T19 | 仍写 1000 组补充执行中 | 真实 Service/SQLite 的 10 场景 × 100 组已通过；普通 8.005 秒、race 95.375 秒。最终全库日志再验证合并后源码，不能把 1001 个测试事件误写成 1001 组。 |
| T21、T22 | 仍写 600 音频故障批次执行中 | 600 组＝10 类故障 × 6 种序号排列 × 10 组样本边界；2400 批次、4560 ACK 重放、800 乱序拒绝、10 次真实数据库重开、300 次补转写失败后恢复；普通和 race 均通过。不是 600 种故障，也不是 600 次进程强杀。 |
| T20、C04 | 摘要源与人工修改描述尚未关联最后交付 | 补 0141 的真实源版本/摘要/完整输入快照、历史来源未知迁移、改稿保留旧摘要并待重生成、源分页；已有 6 个正式 Go 回归。 |
| T49、C04/C05 证据 | 尚未引用最后会议分页/UI 收口 | 补逐字稿、来源、分段的版本绑定分页；长稿局部编辑冲突保草稿、迟到 ACK 隔离、完整导出；前端定向 5 文件/51 测试通过。统一引用 `phase2-meetings-acceptance.md`。 |
| `implementation-backlog.csv` E07/E08 | 仍写最终迁移与集成回归收集中 | 来源真实文件/SQLite、迁移、失败/重开、分页 UI 专项均已完成；在全量门禁结束前可写“专项完成，待统一最终门禁”，不能提前写最终验收完成。 |
| T51、S06 和 E02、S07 等其他代理收口状态 | 个别仍保留“追加收口”或“收口中” | 由主代理按相应所有者的最终交付与全量证据更新；本次不凭相邻模块测试替代这些任务的验证。 |
| T55、Q04 | 完整未签名 NSIS 候选仍待执行 | 必须等实际完整构建、布局、PE、文件摘要与 SOURCE-CANDIDATE 验证通过才更新；签名和实际安装矩阵继续待用户验收。 |
| B00、Q01、最终评分与验证汇总 | 当前等待最终六组门禁 | 只使用汇总脚本接受的同一源码六组回执；按模块查证后再给工程分数，未测试的现场指标保留 pending。 |
| PRD 最后补充入口 | 尚未直接链接最后会议交付说明 | 可加入 `implementation/phase2-meetings-acceptance.md`；其中明确数量口径、摘要来源与长会响应预算。 |

支持矩阵当前对三条默认 ASR 链、可选 realtime、会议完整原稿/来源分页、本地普通文件知识源、实际 MCP 协议、受控浏览器上下文和现场待验的范围描述一致。复核未发现需要扩大能力声明的依据。T16 的 20 轮历史验证有正式循环测试，不需要因只看到一个顶层 Test 而改记为单轮。

本记录是有界脚本与证据口径复核，不替代主代理即将运行的最终全库门禁，也不增加“已安装、已签名、已通过长时间真实使用”等结论。

## 复核后的文档同步

主任务随后已完成完整未签名 NSIS 候选构建：exit=0，安装包 24,988,863 字节，候选源码前后摘要一致。上表是构建前的复核清单；40 项台账、56 项测试计划和 24 行支持矩阵已根据专项交付更新，当前仍明确“统一门禁待执行”，Q04/T55 只新增未签名候选构建事实。新增 `scorecard-phase2-draft.csv` 逐项保留原始五维及权重、第一阶段历史暂评；第二阶段工程五维、均分和实机分全部空置，4.9 仅列为目标。文档更新发生在候选构建完成后，候选内 SOURCE-CANDIDATE 所载文档仍是构建时的原始快照。

## 最终状态补记（r2）

以上表格及首次候选为收口过程的历史记录。最终 r2 六组同源门禁已全部通过，完整新候选为24,986,006字节；正式身份见 [候选记录](evidence/phase2-candidate.json)，该记录取代第一份候选作为本次交付对象。40项台账、56组验收与 [最终评分](scorecard-phase2-final.csv) 已按最终证据更新，草稿评分保留空值仅作历史。工程交付按用户约定评定4.9/5；实机结果仍待用户验收。见 [最终验证](phase2-verification.md) 和 [独立终审](phase2-independent-final-review.md)。
