# 第二阶段最终候选独立复核

复核日期：2026-09-06。复核者为 computer_projects_assets 代理，独立读取原始材料、重算摘要及统计；未调用会写汇总的 finalize-phase2.py，未重跑整库测试，未执行安装或真实业务操作。本轮只新增本文件。

**结论：最终候选与六组门禁对应同一份代码，未发现源码漂移；复核范围内没有仍开放的已知权限、数据或核心流程代码阻断。按主 PRD §1.1 已约定的工程交付口径，具备评定 4.9/5 的条件。该等级不能替代真实环境系统评分、正式签名发布或长期业务验收。**

## 源码和制品身份

实际逐文件检查，而非只比较 JSON 内的声明：

- 当前 HEAD 为 `09978597dc026bfa40ab8d690d64f9bcaef476f3`。交付对象是该 HEAD 上的完整工作区变更，不能只用此提交号代表升级代码。
- [最终源码快照](phase2-source-snapshot.json)的 2,758 个非 docs 文件均与当前文件字节一致；重新枚举 Git 跟踪与未忽略新增文件，路径集合也一致。按清单顺序重算的 SHA-256 为 `e578444c4485345e8aa55dcc2b71e551e2abda0d07627e23cc149e62d50fef17`。
- 六组门禁各自的 source 清单与以上 2,758 项逐项一致；每个回执的 commit、源码摘要、sourceUnchanged、完成状态均匹配。
- 候选内 `SOURCE-CANDIDATE.json` 包含构建时的 3,042 个文件，其中全部非 docs 项与最终门禁逐项一致。含构建时文档的整体摘要为 `e1e3db16e6cd4e8e7abd1623f70840a92b0e67da39f7864a5f008ad8375a1e22`；后续文档更新解释了它与代码摘要的差异，没有用文档豁免掩盖代码变化。
- [代码清单](phase2-code-inventory.json)的源码 2,758、Go 1,499、Go 测试文件 626、web/src 下 TypeScript 文件 464、前端测试文件 219 均复算一致。另有 web/vite.config.ts，归属构建配置，不在 renderer 源文件 464 项内。
- 141 个 SQL 迁移中，原有 123 个与 HEAD 中的文件逐字节相同；新增 0124—0141 共 18 个，无旧迁移改写。
- 实际核验 [候选记录](evidence/phase2-candidate.json)列出的安装包、四个程序和 SHA256SUMS 文件的大小及 SHA-256；逐项核验阶段目录 80 个清单文件，文件集合没有额外或缺失项。
- 安装包为 `release/out/upgrade-20260906-r2/Lunitide-Setup-0.4.67-x64.exe`，24,986,006 字节，SHA-256 `b69b0e97c2d9c03e0e08e273103ffc02c8df21776ed965e2a38cc5994db6771d`。候选构建和独立制品校验退出码均为 0；其声明明确为未签名开发候选、未安装、实机验收 pending-user。

## 六组门禁与原始统计

读取 [最终结果汇总](evidence/phase2-verification-results.json)后，另行解析原始输出，并逐项重算汇总列出的 44 份证据文件的长度与 SHA-256；全部一致。六个 receipt 合计 15 个检查，退出码均为 0，均记录完整开始和结束时间。

| 门禁 | 独立核对结果 |
|---|---|
| Go 与覆盖率 | 原始 JSONL 中 145 个启动包均有终态，无 fail/build-fail；4,766 个测试及子测试通过，28 项测试跳过。coverage.out 共 86,636 条语句、覆盖 46,122 条，重算四舍五入为 53.2%，高于 51% 门槛。 |
| 全库 race 命令 | 145 个启动包均有终态，无 fail/build-fail；4,758 个测试及子测试通过，28 项测试跳过。 |
| 前端 | 契约生成漂移检查、类型检查、测试、生产构建均完成；原始 Vitest JSON 为 219 文件、1,668 项通过、失败 0、跳过 0。 |
| 静态 | 全库 go vet、go build、golangci-lint 均退出 0。 |
| 依赖扫描 | npm audit 各严重度均为 0；原始 govulncheck 仍有 GO-2026-5932 的 golang.org/x/crypto v0.56.0 模块级记录，具有函数调用路径的可达记录为 0。不能称作“所有依赖零漏洞”。 |
| 发布工具 | Windows PowerShell 5 与 PowerShell 7 发布工具回归、发布排除规则检查均退出 0。 |

普通 Go 比 race 多出的 8 项全部位于 cmd/desktop：singleton_windows_test.go 和 watchdog_test.go 既有 `!race` 构建条件使其不进入 race 二进制。这 8 项已通过普通测试，不能声称已经过 race。两组各自另有 28 项显式跳过，涉及真实设备、供应商、模型下载以及受环境条件限制的文件系统测试；均未计入通过。完整名单保留在结果汇总中，不能把完整 `./...` 命令解释为所有平台和所有可选实机用例都已运行。

## 已知整改与工程评分准入

核对了 40 项实施台账、56 组测试计划、24 行支持矩阵、模块专项记录和最终评分表。代码工作、自动化证据与用户现场责任分别登记，没有把尚未实施的代码转记为真机任务。曾复现的计划执行、发布证据、资产幂等、电脑控制撤权/审计、组织停用和资源归属、会议长正文与聚合容量等问题，均有最终实现记录和正式正确行为回归；最新六组门禁的同源结果涵盖这些测试。其他领域的专项事实沿用其责任代理的实现复核与原始回归，本次没有声称重新逐行审计全部代码。

[最终工程评分 CSV](scorecard-phase2-final.csv)包含 24 个唯一模块；模块顺序、权重及整改前五维与基线 CSV 一致，权重合计 35。逐行重算第二阶段五维与模块均分，均为 4.9；加权结果也为 4.9。专项证据文件均存在；实机系统评分 24 行全部为空。

该工程交付等级成立的依据是约定工作完成、反例关闭、质量门禁和候选交付一致，以及没有仍开放的已知硬阻断，不能由测试数量、覆盖率或“通过率”换算。原 PRD §3、§10—11 对完整实机系统的量表与观察门槛仍保留；目前不能据工程等级宣称所有模块的真实使用质量均已测得 4.9，更不能承诺永不报错或宕机。后续发现新的权限、数据或核心流程阻断，应重新打开原任务并撤回相应完成判断。

## 用户交接及本次发现

[用户验收 JSON](user-acceptance.json)与最终候选的版本、提交、整体源码摘要、安装包摘要和阶段清单摘要一致。15 个用例状态均为 pending，实测环境未填写，overallAcceptance 为 pending、score 为 null；[支持矩阵](support-matrix.csv)的 24 行实机状态也都明确待验。真实三默认语音链与可选 realtime、现场桌面、外部服务、跨设备、安装签名和长期业务观察没有冒充已验。

本次发现一处文档门槛漂移：Q04-SOAK 最初仍写“8h or longer”，低于交接说明及 PRD 约定。已反馈主任务并再次读取核实修正：现明确 72 小时混合负载、14 天受控观察、至少 3,000 次核心有效操作，新增 criteriaReference；状态、actual、evidence 和系统评分仍保持未填写。这是已纠正的交接缺口，没有通过降低原门槛消除。

再次核查发现通用 release/manual-acceptance.template.json 和 Test-Soak.ps1 仍保留 8 小时默认；该工具支持显式设置时长，结果中的 workloadAcceptance 固定为 pending-manual-evidence，资源观察只产生 observed-stable 或 review-required，不自动授予业务通过。最终交接说明已明确本次必须使用绑定候选且包含完整门槛的 user-acceptance.json，通用 8 小时默认只供短时调试。因此此默认值在已澄清的工具范围内不构成本次未关闭代码阻断，也不构成 72 小时验收已完成的证据。

当前未发现需要阻断工程候选交付的未关闭项。正式发布者签名、真实安装/升级/卸载、真实供应商与硬件、业务质量样本和 72 小时/14 天观察仍由用户执行并补证据；本次只读复核及未签名候选均不能替代这些验收。
