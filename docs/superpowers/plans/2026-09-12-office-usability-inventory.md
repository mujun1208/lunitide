# Office Usability Inventory Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans and test-driven-development. Stay on `feat/prd-v7-s1-continuity`. No worktree. No finishing-a-development-branch. Do not commit unless the user asks. Do not edit `c:\Users\mujun\.cursor\plans\office_usability_inventory_fc90ca11.plan.md`.

**Goal:** Close remaining Studio dead ends (I1–I5). Pack 100% ≠ first-ship / full PRD 100%.

**Spec:** [docs/superpowers/specs/2026-09-12-office-usability-inventory.md](../specs/2026-09-12-office-usability-inventory.md)

## Global Constraints

- Stay on `feat/prd-v7-s1-continuity`. No commit / push / VERSION / pack unless asked.
- Frozen `token_ledger`; `html.gen` stays `penalty-shootout|timer|checklist`.
- Do not invent savings %, 机密, 管理层 defaults, or competitor scores.
- `designerReviewed` stays `0`. Do not put Presenton on Generate.
- Formal still skips honest unsupported/missing `target-*` / `visual-model` / `pdfa`.
- Live app stays 0.4.75 until rebuild.
- Windows tests need `required_permissions: ["all"]`.
- Regenerate bridge via `npm --prefix web run generate:bridge` if OfficeCheckDTO enum changes. Do not hand-edit generated files.

---

### Task 1: I1 status labels + snapshot fidelity

### Task 2: I2 probe optional tools

### Task 3: I3 target-app labels

### Task 4: I4 visible form errors

### Task 5: I5 import/generate notices

### Task 6: Gate and canvas
