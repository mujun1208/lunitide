# GitHub Online Overlay Update Implementation Plan

**Goal:** Check GitHub `latest.json` and download the Setup on「立即升级」, then reuse silent overlay install. Also keep weekly-report `office.generate` on the routed tool table.

**Spec:** `docs/superpowers/specs/2026-09-14-github-online-overlay-update-design.md`

Shipped on this branch as 0.4.83: `networkpolicy.Copy`, `internal/desktopupdate`, `FeedLookup` + `CompositeLookup`, `AppUpdateBanner`, `Build-Release.ps1` `latest.json` + `Publish-GitHubRelease`, task-route office/weekly/`不打开`/`plan.run` allow.
