# M10 第一梯队（T1 增强域）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地 M10 三个增强域——记忆提名工作流、专家场景卡、技能 12 分类——走通 schema→迁移→服务→handler→codegen→UI 全链路。

**Architecture:** 零骨架改造。提名 = 候选的富化包装（新表 memory_nominations 关联现有 candidate，不动 0061）；场景卡为专家域新聚合（expert_scenario_cards + 多对多挂载）；技能分类为加列迁移（append-only DEFAULT）。Bridge 走 x-method schema → generate:bridge → engine.go 注册 → client.ts 链路。

**Tech Stack:** Go (internal/app, internal/m8app, internal/skillapp, internal/storage/sqlite) + React/TS (web/src) + SQLite migrations 0071~0073。

---

## Task 1: 迁移 0071 记忆提名表

**Files:**
- Create: `migrations/0071_m10_memory_nomination.sql`
- Modify: `internal/storage/sqlite/store.go` (manifest 追加)

- [x] **Step 1: 写迁移 SQL**

```sql
-- 0071 M10 记忆提名工作流：nomination 是 candidate 的富化包装——
-- 提名理由与来源会话跟随候选，确认/拒绝仍走 0061 的显式 token 链路
--（FR-02/FR-11 不变量不变）。

CREATE TABLE memory_nominations (
    nomination_id TEXT PRIMARY KEY CHECK (length(nomination_id) = 26 AND substr(nomination_id, 1, 1) GLOB '[0-7]' AND nomination_id NOT GLOB '*[^0123456789ABCDEFGHJKMNPQRSTVWXYZ]*'),
    candidate_id TEXT NOT NULL REFERENCES memory_candidates(candidate_id),
    nominator TEXT NOT NULL CHECK (length(nominator) BETWEEN 1 AND 128),
    reason TEXT NOT NULL CHECK (length(reason) BETWEEN 1 AND 2048),
    source_session_id TEXT,
    state TEXT NOT NULL DEFAULT 'nominated' CHECK (state IN ('nominated','decided','withdrawn')),
    decided_at TEXT,
    created_at TEXT NOT NULL,
    UNIQUE (candidate_id)
);

CREATE INDEX idx_nom_state ON memory_nominations(state, created_at);
```

- [x] **Step 2: 计算 SHA-256 并登记 manifest**

Run: `Get-FileHash -Algorithm SHA256 migrations/0071_m10_memory_nomination.sql`
在 `internal/storage/sqlite/store.go` manifest 数组末尾追加 `{"0071_m10_memory_nomination.sql", "<digest>"}`。

- [x] **Step 3: 验证迁移跑通**

Run: `go test ./internal/storage/sqlite/ -run TestMigrations -v` → PASS

- [x] **Step 4: Commit** `git add migrations/ internal/storage/sqlite/store.go && git commit -m "feat(m10): migration 0071 memory nominations"`

## Task 2: 记忆提名服务层

**Files:**
- Create: `internal/m8app/memory_nomination.go`
- Test: `internal/m8app/memory_nomination_test.go`

- [x] **Step 1: 写失败测试**（nominate 创建提名+候选、withdraw 撤回、ListNominations 列表、decided 状态联动）
- [x] **Step 2: 实现**（NominateTx 接口扩展 MemoryTx：PutNomination/GetNomination/ListNominationsByState/TransitionNomination；Nominate 复用 ProposeCandidate 后写提名行；Withdraw 仅 nominated 态可撤；confirm/reject 时联动 decided——在 handler 层联动而非改 ConfirmCandidate，保持 0061 服务零侵入）
- [x] **Step 3: `go test ./internal/m8app/ -v` → PASS**
- [x] **Step 4: Commit** `feat(m10): memory nomination service`

## Task 3: 记忆提名 bridge schema + handler + 注册

**Files:**
- Create: `api/bridge/v1/memory.nominate.schema.json`, `memory.nomination.list.schema.json`, `memory.nomination.withdraw.schema.json`
- Create: `internal/app/m10_memory_handlers.go`
- Modify: `internal/app/engine.go`（注册 3 方法 + m10nomination 服务字段）
- Modify: `api/bridge/v1/envelope.schema.json`（method enum 按序插入）

- [x] **Step 1: 写 3 个 schema JSON**（x-method + x-examples 正反例；错误码 M10-ME-001~004：提名不存在/已决/已撤回/理由无效）
- [x] **Step 2: `npm run generate:bridge`** → generated/bridge.ts + schema_generated.go 更新；x-enabled 若断言失败则同步列表
- [x] **Step 3: 写 handler**（decodePayload → nil 服务检查 STORAGE_UNAVAILABLE → 服务调用 → m10MemoryFailure 错误映射）
- [x] **Step 4: engine.go 注册 + engine_test.go 编译验证**
- [x] **Step 5: `go test ./internal/app/ -v && npm run verify:bridge` → PASS**
- [x] **Step 6: Commit** `feat(m10): memory nomination bridge API`

## Task 4: 记忆中枢 UI（3-tab + 侧边栏提升）

**Files:**
- Modify: `web/src/memory/MemoryPage.tsx`（3-tab：待决提名/已确认记忆/召回轨迹）
- Modify: `web/src/bridge/client.ts`（nominate/nominationList/nominationWithdraw 接口 + MutationMethod 追加 memory.nominate|memory.nomination.withdraw）
- Modify: `web/src/App.tsx`（Page 联合类型加 'memory'，侧边栏项目管理分组加"记忆中枢"入口，移除设置页内嵌——设置页保留入口跳转）
- Test: `web/src/memory/MemoryPage.test.tsx` 扩展

- [x] **Step 1: 扩展测试**（3-tab 渲染、提名列表加载、确认/拒绝动作、撤回）
- [x] **Step 2: 实现 client.ts 接口 + MemoryPage 3-tab**
- [x] **Step 3: App.tsx 路由接入**
- [x] **Step 4: `npm --prefix web run test -- MemoryPage` → PASS；`npx tsc --noEmit` → 0 错误**
- [x] **Step 5: Commit** `feat(m10): memory hub UI with nominations tab`

## Task 5: 迁移 0072 专家场景卡 + 服务 + bridge

**Files:**
- Create: `migrations/0072_m10_expert_scenario.sql`（expert_scenario_cards：scenario_card_id/expert_id FK/title/summary/phase_key 复用 9 阶段枚举/scenario_json/digest/state/created_at，UNIQUE(expert_id,title)）
- Create: `internal/m8app/expert_scenario.go` + test（Create/List/Delete/场景卡 digest 校验，删除仅 soft-state archived）
- Create: `api/bridge/v1/expert.scenario.create|list|delete.schema.json`
- Create: `internal/app/m10_expert_handlers.go`
- Modify: `internal/app/engine.go`, `envelope.schema.json`, `web/src/bridge/client.ts`

- [x] **Step 1: 迁移 SQL + manifest 登记（同 Task 1 模式）**
- [x] **Step 2: 服务层 TDD**（M10-EX-001~003：场景卡不存在/标题冲突/digest 不匹配）
- [x] **Step 3: schema + codegen + handler + 注册**
- [x] **Step 4: 验证**（`go test ./internal/m8app/ ./internal/app/`、`npm run verify:bridge`）
- [x] **Step 5: Commit** `feat(m10): expert scenario cards`

## Task 6: 专家中心 UI 场景卡区块

**Files:**
- Modify: `web/src/expert/ExpertCenterPage.tsx`（专家详情抽屉加场景卡列表 + 创建/删除）
- Test: `web/src/expert/ExpertCenterPage.test.tsx`（若无需新建）

- [x] **Step 1: 测试先行**（场景卡渲染/创建表单/删除确认）
- [x] **Step 2: 实现 + `npx tsc --noEmit` + 测试 PASS**
- [x] **Step 3: Commit** `feat(m10): expert center scenario cards UI`

## Task 7: 迁移 0073 技能分类 + 域模型 + 服务

**Files:**
- Create: `migrations/0073_m10_skill_category.sql`（`ALTER TABLE skills ADD COLUMN category TEXT NOT NULL DEFAULT 'uncategorized' CHECK (category IN (12 枚举))`）
- Modify: `internal/domain/skill/skill.go`（Category 字段 + SkillCategory 常量 12 个 + Validate 分支）
- Modify: `internal/skillapp/service.go` + catalog.go（List 按 category 过滤；Create/Update 接受 category；catalog 模板补 category）
- Test: `internal/skillapp/service_test.go` 扩展

- [x] **Step 1: 12 分类定义**（coding/writing/data/automation/design/office/web/file/system/learning/communication/uncategorized）
- [x] **Step 2: 迁移 + manifest + 域模型 + 服务 TDD**
- [x] **Step 3: `go test ./internal/skillapp/ ./internal/storage/sqlite/` → PASS**
- [x] **Step 4: Commit** `feat(m10): skill 12-category classification`

## Task 8: 技能分类 bridge 扩展 + UI 筛选

**Files:**
- Modify: `api/bridge/v1/skill.create/update/list.schema.json`（category 参数 + 过滤）
- Modify: `internal/app/skill_handlers.go`（读写 category）
- Modify: `web/src/bridge/client.ts`、`web/src/skill/SkillPage.tsx`（12 分类筛选条，默认"全部"）
- Test: `web/src/skill/SkillPage.test.tsx` 扩展

- [x] **Step 1: schema 修改 + `npm run generate:bridge`**
- [x] **Step 2: handler + client + SkillPage 筛选条**
- [x] **Step 3: 全量验证**（`go test ./...`、`npm --prefix web run test`、`npm run verify:bridge` 零回归）
- [x] **Step 4: Commit** `feat(m10): skill category filter UI`

## Task 9: T1 收尾全量回归

- [x] **Step 1: `go test ./...` 全绿**
- [x] **Step 2: `npm --prefix web run test` 全绿（294+ 零回归）**
- [x] **Step 3: `npm --prefix web run typecheck` 0 错误**
- [x] **Step 4: Commit 收尾** `chore(m10): T1 wave regression green`
