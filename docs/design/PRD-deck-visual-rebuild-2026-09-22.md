# PRD：演示文稿视觉重建（换设计源，不重写 Office Studio）

- 日期：2026-09-22（同日按 `D:\PPT模板库\工作总结` 19 套实文件修订）
- 状态：方案，**按真实模板修订后待确认，尚未开工**
- 触发：用户反馈「做出来的 PPT 非常难看，选项页复杂」，并要求对标 Kimi PPT
- 输入材料：Kimi 对话导出（`Harness 功能清单.docx`）；51miz 个人会员模板库 `D:\PPT模板库`（首批用「工作总结」）；本次代码核查
- 前置 PRD：`PRD-office-quality-commercial-2026-09-11.md`、`PRD-office-studio-v1.md`、`PRD-office-platform-closed-loop-2026-09-12.md`

---

## 0. 一句话结论

难看的根因不是模型，也不是 OOXML 写入能力，而是**设计源是 Go 代码里手写的 DrawingML 坐标，不是设计师能改的设计产物**。

对齐 Kimi 的「经典模板模式」，主路径是：**从用户模板库克隆选中的页，只替换页内已有文本框里的样例文案，几何、图片、主题原样保留。**

| | 模式 A：克隆页 + 替换样例文案（主力） | 模式 B：自由布局（补位，后做） |
|---|---|---|
| 设计源 | `D:\PPT模板库` 里的设计师 PPTX | HTML 主题包 |
| 替换对象 | 页内**所有样例文本框**，占位符只是其中一种 | 整页重排 |
| 不替换 | 图片、装饰形状、母版、主题、已烧进图片的文字 | — |
| 观感上限 | = 所选模板页 | = 主题 CSS |
| 可编辑 | 原生全可编辑 | 文字可改、形状不可改 |

**两件实测事实决定了不能只填占位符（见 3.3）：**

1. 用户已改名、会实际选用的 4 套（`工作总结.pptx`、`月度工作总结.pptx`、`财务工作总结.pptx`、`岗位竞聘.pptx`）**占位符数为 0**。正文写在名为「文本框 / 矩形」的散装形状里，样例文案是「添加标题」「此处添加详细文本描述」。
2. 即便占位符齐全的套（7/19），占位符里也多是「目录 / 01 / 完成情况」这种短饰条；真正的标题、正文、指标在占位符**外面**。只填占位符会得到一套没换内容的精美空壳。

两条路都保留现有 Spec、OOXML 写入、版本、QA、导出体系。这不是重做 Office Studio，改动集中在「设计源」这一层。

---

## 1. 核查结论：为什么前几轮没把「好看」做出来

`PRD-office-quality-commercial-2026-09-11.md` 的诊断是对的（它已经提出「统一品牌规则 + 语义版式库 + 模板编译 + 视觉验收」，并设计了四组对照实验）。
问题是落地时，其中三件事**从能力降级成了声明**。以下都有代码证据。

### 1.1 「三套风格」实际上是一套

`internal/officestudio/templates/ops-clear.json` 与 `brand-pitch.json` **除 `templateId` 外字节完全相同**（本次逐字节比对确认）：同样的 `slots`、同样的 `constraints`。
`editorial-report.json` 仅有极小差异。三套模板的真实差异只是 `internal/officestudio/theme.go` 里 `StarterBrand(id)` 的几个颜色字段。

后果：用户在「风格」页签里选择，**版面骨架一点没变，只换了颜色**。这直接解释了「选项很多但没用」的体感。
模板 JSON 里没有任何配色、字号、网格、间距信息——它不是设计产物，是一张 slot 白名单。

### 1.2 视觉验收被测试钉死在「未校准」

- `internal/officestudio/quality.go:116` 硬编码输出 `visualScore=uncalibrated`
- `internal/officestudio/variants.go:43` 硬编码输出 `designerReviewed=0`
- `quality_test.go:19`、`layoutplan_test.go:170-177` **用测试禁止**声明 `designer-checked` 或 `designerReviewed=36`

这套自律是对的（不许把工程变体冒充设计师复核），但它同时说明：**视觉闭环从未建成**，系统自己知道它没被设计师看过。
现有「渲染」只到 LibreOffice → PDF 预览，渲染结果没有回流去修版面。

### 1.3 上一版推荐的备选渲染后端从未启用

`internal/officestudio/adapter.go:98,102`：Presenton 与 PptxGenJS 仅是 `os.Getenv("LUNITIDE_PRESENTON")` / `os.Getenv("LUNITIDE_PPTXGENJS")` 探针，用于对照，不在产品路径上。

### 1.4 版式引擎只会切页，不会排版

`internal/officestudio/layoutplan.go`：溢出处理是「拆成更多页，续页标题追加（续）」；`splitOverlongBullets(..., 300)` 按字符切；`layoutItemLimit` 12（timeline/metrics 为 6）。
没有字号自适应、没有网格、没有留白/对比度优化。内容一多就变成「标题（续）（续）」，读起来是机器产物而不是设计稿。

### 1.5 素材层为零

有 `addSlideImages`（EMU 定位、contain/cover），但没有图标库、没有自动配图、没有插画、没有背景图形。
纯文字 bullet + 深蓝底（`DefaultBrand()` navy `#0B1F3A` + Calibri / 微软雅黑）就是 2003 年的观感。

### 1.6 两条画质不同的生成路径，用户多数走在丑的那条

| | 聊天 `pptx.gen` | 工作台 `office.generate` |
|---|---|---|
| 生成器 | `officetools.GenPptx`，**单一 `slideLayout1.xml` 复用给所有页** | `GenStudioPptxThemed`，16 种语义版式 |
| 版式数 | 3（title / section / content） | 16 |
| 图表/图片 | 无 | 原生 DrawingML 图表 + 嵌入工作簿 |

聊天里的「PPT专家」「slide-builder」技能走的是第一条，也就是最丑的一条；而且 `pptGenBlocked` 的九步流水线闸门让它**又慢又丑**。

### 1.7 选项复杂，是把工程内部状态摆到了用户面前

现有「可选设置」四页签清单（实测标签原文）：

- **简报**：受众、用途、目标页数、密级，外加**逐页**填写「页标题 / 页目的 / 页结论」
- **风格**：三个单选，页面上写着「工程变体，非设计师已检 36」
- **品牌**：品牌编号、西文字体、中文字体、主色、来源、许可、摘要、标识摘要，提示「登记品牌需要编号、来源、许可和 **64 位摘要**」，并声明「一级导入只登记颜色、字体与授权记录，**不还原 PPT 母版**」
- **说明**：四条工程免责声明 + 阶段条

最严重的一条：**工作台里没有生成按钮**——界面上明写「生成在右侧对话里进行；上方预览和工作台阶段不是生成按钮」。
用户面对一个要填 20 多个字段、却不能生成的工作台。这不是选项多的问题，是信息架构错位。

### 1.8 未能核实的一项

我**没有找到**上一版 PRD 设计的四组对照实验的人工盲评结果记录。`testdata/office-quality/*.json` 与 `qa_harness_test.go` 存在，但 `visualScore=uncalibrated` 被硬编码这一事实倾向说明人工盲评没有落地。不下断言，但这个实验必须先跑（见 6.1）。

---

### 1.9 实测证据（P0 探针，2026-09-22）

用 `_scratch/p0deck` 生成当前管线的真实产物并反向解析：

```
slides=5  slideLayouts=1  media=0  embeddedFonts=0
slide1..5: keep=5..17  (no placeholders)
VERDICT  fillable=0/5  decorated=5/5  bare=5
```

关键事实：**当前输出里一个占位符（placeholder）都没有**，每页是 5–17 个带绝对坐标的散装 shape，5 页共用 1 个 `slideLayout`，无媒体、无内嵌字体，整包 12 KiB。

这比「难看」更严重：产出物在 PowerPoint 里**是一张画，不是一份演示文稿**——

| 用户操作 | 当前产物的结果 |
|---|---|
| 大纲视图 | 空白（文字不在占位符里） |
| 重置幻灯片 | 无反应 |
| 设计 → 换主题 | 只换配色，版式不动 |
| 切换版式 / 换母版 | 不重排 |

所以「可编辑」现在只是字面成立（能双击改字），**结构性可编辑是缺失的**。模式 A 不解决「换母版重排」——设计师页本身也常常没有占位符。它解决的是观感：成品长得像所选模板，文字能在原格子里改。

---

## 2. 对标：Kimi 好看在哪一层

用户提供的 Kimi 对话自己给出了六层模型，其中一句是判断依据：
> 模型只决定「内容好不好」，真正决定「PPT 好看」的是——模板库 + 版式算法 + Office OpenXML 文件引擎 + 无头浏览器渲染回路 + 截图圈选交互。

把六层对到本项目现状：

| 层 | Kimi | lunitide 现状 | 判断 |
|---|---|---|---|
| 内容结构化 | 大纲树 / 结构化中间格式 | ✅ `officestudio.Spec` + `Brief` + `NarrativeNode` 已相当完整 | 不是瓶颈 |
| 模板与素材 | 设计师模板库、图标、配图、图表主题；支持上传自定义 PPTX 模板、参考图复刻 | ❌ 3 张 slot 白名单 + 3 组写死配色，无图标无配图 | **主瓶颈** |
| 版式引擎 | 自动分页 + 字号自适应 + 行距/对比度/留白 | ⚠️ 只有拆页 | **主瓶颈** |
| 文件格式引擎 | 浏览器 OOXML writer / 本地 DrawingML | ✅ 自写 OOXML、原生图表、嵌入工作簿 | **已是强项** |
| Agent 运行时 | 300 子 Agent | ✅ 工具链、预算、隔离已有 | 不是瓶颈 |
| 预览与编辑闭环 | 在线编辑 + 截图圈选改 | ⚠️ 结构预览 + LibreOffice PDF，无视觉回流 | 次瓶颈 |

检索补充到的技术细节（对话里没有）：

1. **Kimi Slides 的中间格式是 PPTD（逐页 YAML）**，含页内元素动画与预设主题，配本地浏览器编辑器，导出走浏览器 OOXML writer 或本地 DrawingML（依据：公开的逆向实现 Open-Deck-Skill）。也就是说 Kimi 的设计源是**一份富声明式文档 + 浏览器渲染**，不是代码里算坐标。
2. **Kimi PPT 官方口径**：双模式（经典模板模式＝模板库+AI 填充；智能布局模式＝AI 自主排版）；元素原生可编辑，复杂图表也可调；支持 PDF/Word/Excel/Markdown/图片/自定义 PPTX 模板输入；可导出 PPTX/PNG。
3. **业界另一条主流路线**：HTML/CSS 做设计源 → 无头浏览器截图作底图 → 用 `getBoundingClientRect` 提取文字坐标 → 生成可编辑文本框钉在底图上。视觉 100% 保真、文字可改，代价是形状不可编辑。对中文还有隐性优势：**绕开 LibreOffice / PowerPoint 字体度量不一致导致的串版**。

结论：差距不在模型，在设计源与素材。Kimi 的「经典模板模式」对到本方案的模式 A；「智能布局模式」对到模式 B，且模式 B 排在模板填充验证通过之后。

---

## 3. 可行性判定

### 3.0 决定性发现：部件替换架构已经存在

`internal/officetools/studio_pptx.go:98-132` 的实际流程是：

1. `base, err := GenPptx(title, baseSlides)` —— 先生成一个**自己造的** pptx 包
2. `zip.NewReader(bytes.NewReader(base), ...)` —— 打开它
3. 把 `ppt/theme/theme1.xml` 与每个 `ppt/slides/slideN.xml` **替换**为手写 XML
4. 其余部件 `zw.Copy(f)` 原样拷贝

也就是说「打开一个 pptx 包、替换其中的部件、其余透传」这套架构**已经在产品主路径上运行**。
它不好看的唯一原因是第 1 步的 base 来自自己的 `GenPptx`，而不是一份设计师做的模板文件。

部件替换架构可以复用，但**不能沿用它现在的替换方式**。它替换的是整份 `slideN.xml`。设计师模板要的是反过来：`slideN.xml` 整份保留，只改其中 `<a:t>` 文本节点。

这不是「换一个 base 变量」就能完成的。要新增的是：模板库索引、样例文案识别、按槽位改文本、按容量决定缩字或再克隆一页。OOXML 打包、主题透传、图表与嵌入工作簿仍复用。

### 3.1 其余在手资产

**可行，且比预期容易**，因为三个关键资产已经在手：

1. **已有无头 WebView2 渲染工作器**。`internal/webviewhost/diagram_worker_windows.go` 已能在隐藏 WebView2 中通过虚拟主机（`diagram.lunitide.local`）加载本地 HTML、执行 JS、回传结果（当前回传 SVG，见 `internal/diagramrender/handler.go` 的 `Result.SVG`）。改成幻灯片渲染器，只差 WebView2 的 `CapturePreview` 截图调用，同一套绑定内即可。
2. **刚建成的隔离预览源** `preview.lunitide.local`（`internal/webviewhost/preview_origin.go`，严格 CSP：允许脚本、禁止外联与 bridge），正好是渲染 HTML 幻灯片的安全容器。
3. **OOXML 写入、原生图表、版本链、QA、导出与成套导出**均已成熟，全部复用。

**需要降低期望的部分**

- 「视觉媲美」与「完全原生可编辑」只在**模式 A** 下可兼得；模式 B 仍须二选一（见 4.4）。
- 模式 A 的观感上限就是模板本身的水平，产品无法超过素材。模板库的质量直接等于产品的质量。
- 模板里没有的版式，本期用「最接近的一页」克隆，不切到 HTML 重排。宁肯版式近似，也不要一页模板风、一页程序风。
- 字体沿用模板引用。本机缺字只报告，不把商用字体打进安装包。

### 3.2 素材与授权边界（个人自用）

用户为觅知网（51miz）个人版终身会员，明确**仅自用、不商用**。据此定边界：

| | 规则 |
|---|---|
| 模板与素材存放 | 本地用户目录（如 `%LOCALAPPDATA%/Lunitide/templates/`），**不进代码仓库、不打包进安装包、不随 release 分发** |
| 产品内置 | 本期不内置模板，也不内置 HTML 主题。安装包里没有 pptx 素材 |
| 模板来源标注 | 模板库条目记录来源与授权口径，复用现有 `license` 字段惯例 |
| 将来要商用 | 只需更换素材源，架构不变——这是把素材做成可插拔的理由 |

这条边界让「自用可以随便用好素材」和「将来可商用」不冲突，且不需要两套架构。

模板库根目录由用户指定为 **`D:\PPT模板库`**，按子文件夹分类。产品只索引、不复制进仓库、不打进安装包。首批语料是子文件夹 **`工作总结`**，以后新增文件夹即新增分类，不改架构。

### 3.3 工作总结 19 套实测（2026-09-22）

探针打开每一套的 zip，统计占位符、装饰形状、媒体、字体，并抽样读出文本框原文。

| 类别 | 套数 | 代表 | 占位符 | 正文实际在哪 |
|---|---|---|---|---|
| 用户已改名、会直接选用 | 4 | `工作总结.pptx`（31 页）、`月度工作总结.pptx`（31 页）、`财务工作总结.pptx`（33 页）、`岗位竞聘.pptx`（33 页） | **全部 0** | 散装「文本框 / 矩形」。例：`工作总结.pptx` 第 4 页是 6 组「添加标题」+「此处添加详细文本描述，建议与标题相关」 |
| 占位符齐全 | 7 | `76f50305…pptx`（28 页，28/28）、另有 62 页、34 页、30 页、29 页等 | 几乎每页都有 | 占位符只放短饰条（「年中工作总结」「目录」「01」「完成情况」）。标题、正文、`XX%`、`30+` 在占位符外的矩形里 |
| 占位符零星 | 1 | `253b2990…pptx`（30 页，8/30） | 少数页 | 封面写着「汇报人：觅觅」「2024年觅知企业项目工作汇报」；内页是「关键词标题」+「在这里输入你的正文…」 |
| 占位符为 0 的其余哈希文件 | 7 | 含最大的 78.9MB / 29 页 | 0 | 同第一类，散装形状或文字烧进图片 |

合计 19 套、约 590 页。另有两条结构事实：

- **纯图页存在。** `工作总结.pptx` 第 2 页、第 8 页 `text-shapes=0`：文字已经画进图片，改 XML 文本改不到画面上。这类页只能原样当章节隔页用，不能当内容槽。
- **字体是模板自带引用，不是产品内嵌。** 抽样见到思源黑体、思源宋体 CN、阿里巴巴普惠体、微软雅黑、OPPOSans、PingFang SC。PingFang 是 macOS 字体，Windows 上会回退，必须在生成前报出来，不能静默变成另一种字。

因此模式 A 的填充单位不是 OOXML 占位符，而是**「页上每一个装了样例文案的文本形状」**。占位符只是这个集合的子集。

---

## 4. 方案

### 4.1 架构：只换设计源那一层，两种设计源并存

```
目标 → Deck IR（内容，无坐标）
          │
          ├── 选中模板里的页 ──→ 模式 A
          │                     克隆这些页 → 只替换样例文案（含占位符与散装文本框）
          │                     → 图片 / 装饰 / 主题透传 → 原生可编辑 pptx
          │
          └── 无匹配模板页 ──→ 模式 B：自由布局
                                主题包(HTML/CSS) → 幻灯片 HTML
                                → 无头 WebView2 渲染 → 截图
                                → 视觉检查 → 回流修版
                                → 保真档（底图+文本层）/ 原生档（DrawingML）
```

两模式共享：Deck IR、自适应排版（4.5）、视觉检查、质检与版本。
复用不动：`Spec`/`Brief`/`NarrativeNode`、facts 与引用、patch（改文案/换图/改图表）、版本与接受、`office.artifact.*` 全部 bridge、成套导出、存储用量。

### 4.1a 模式 A：克隆页，替换样例文案（主力）

输入是用户一句话目标（可附参考材料）+ 从模板库选中的一套。输出是一份新的 pptx：母版、主题、图片、装饰形状来自模板，文字来自这次的内容。

**库**

- 根目录 `D:\PPT模板库`，一级子文件夹 = 分类。首期只承诺「工作总结」。
- 索引存在本机应用数据里，记录：路径、分类、文件哈希、页数、每页槽位清单、字体清单、是否含 `docProps/thumbnail`。
- 同一内容的两份文件（例如 `月度工作总结.pptx` 与一份同为 31 页、8.3MB 的哈希文件）按结构哈希折叠成一条，避免库里出现两张一样的卡片。
- 模板不进 git、不进安装包。换电脑时用户自己带这个文件夹，或在设置里改根目录。

**一页上的槽，按现有文案归类，不按占位符标签归类**

| 槽 | 怎么认 | 例子（实测原文） | 填什么 |
|---|---|---|---|
| 标题槽 | 短中文，或文案就是「添加标题」 | 「添加标题」「工作总结概述」 | 大纲页标题 |
| 正文槽 | 含「此处添加」「在这里输入」「建议与标题相关」「关键词：」 | 「此处添加详细文本描述，建议与标题相关」 | 该要点的说明，字数预算 = 样例字数 |
| 指标值 | 文案匹配数字、`XX%`、`30+`、`95%+` | 「30+」「95%+」「6」 | 用户数据；没有数据就留空并删掉这一组，不编造 |
| 指标名 | 紧挨指标值的 2–6 字中文 | 「核心任务」「目标达成率」 | 指标名称 |
| 目录项 | 「01」这类序号 + 相邻短标题 | 「01」「上半年工作回顾」 | 大纲章节名 |
| 封面元信息 | 「汇报人」「部门」「时间」「主讲人」 | 「汇报人：觅知网」「部门：市场部」 | 用户身份与日期 |
| 英文对照行 | 纯拉丁、且旁边有中文标题 | `Market share steady growth` | 由中文标题生成一句短英文；用户可关 |
| 不改 | 文案是「LOGO」、或该形状没有文本、或该页 `text-shapes=0` | 纯图页、图片、装饰线 | 原样保留 |

形状的 `cNvPr@name`（「文本框 32」「矩形 15」「觅知网-不倒翁ID:…」）只作调试信息，**不作为槽类型**。觅知的 ID 写在形状名字里，画面上不一定可见；可见的「汇报人：觅知网」属于封面元信息，要换成用户的名字。图片水印不自动擦除。

**生成步骤**

1. 模型只产出 Deck IR：封面信息、章节、每节若干要点、可选指标。IR 仍然不含坐标和颜色。
2. 在所选模板里为每个章节挑一页版式最接近的样例页（封面、目录、要点宫格、指标、结尾）。挑页依据是槽的构成，不是文件名。用户在大纲步可以换页。
3. 新建 pptx = 模板包的拷贝，只保留被挑中的页（以及这些页依赖的 layout、master、theme、media）。被丢掉的样例页不出现在成品里。
4. 对保留页，只替换槽里的 `<a:t>`。不新增形状，不改位置，不改 `slideN.xml` 的其余节点。
5. 容量：正文槽的预算就是样例文案的字数（设计师已经按这个框排过）。超出则先降该形状的字号（只降这个形状，有下限），仍超出则把多余要点克隆同一页再排，克隆页复用同一 layout 与装饰。不允许把 200 字塞进「添加标题」那个 4 字的框。
6. 纯图页：若用户大纲需要一个章节隔页，可以原样插入，但界面标明「此页文字在图片里，不能改字」。不在图片上叠一层新文本框冒充可编辑。
7. 字体：开打前用本机字体列表核对主题与形状里引用的字体。缺 PingFang SC 这类字体时，成品仍生成，质检里写明「本机没有该字体，PowerPoint 会回退」。不静默把引用改成雅黑，除非用户在该套模板上点了「缺字体时改用微软雅黑」。

P0 要证明的就是第 4 步：拿 `工作总结.pptx` 第 4 页，把 6 个「添加标题」和 6 句「此处添加…」换成一段真实经营汇报，其余字节不动；用 PowerPoint 打开，装饰还在、中文不串出格子。同时拿 `76f50305…pptx` 第 8 页做对照，确认占位符页和散装文本框页走的是同一套替换，而不是两条代码。

### 4.2 Deck IR（内容层）

在现有 `officestudio.Spec` 上收敛，明确一条硬约束：**IR 不含任何坐标、字号、颜色**。
IR 只描述「这一页是什么语义版式、有什么内容、引用哪些事实」。它是唯一的内容真相，可版本化、可 diff、可回填。
现有 `Slide` 的 `purpose` / `claim` / `evidenceRefs` / `comparison` / `metrics` 保留，`layoutTrace` 从「坐标痕迹」改为「排版决策痕迹」。

### 4.3 主题包（设计层，设计师可改）

目录形态：

```
internal/officestudio/themes/<id>/
  theme.json      # 配色、字体、字号阶梯、网格、间距、圆角、图标风格、许可声明
  theme.css       # 设计系统实现
  slots/cover.html  section.html  content.html  metrics.html
        compare.html  timeline.html  process.html  chart.html
        quote.html  table.html  evidence.html  closing.html
```

- 自适应用 CSS Grid + `clamp()` 做字号阶梯，容器查询做密度适配。
- **验收硬标准：任意两套主题的同名 slot HTML 结构必须不同。** 这一条直接把 1.1 的问题钉死——防止主题再退化成「同一骨架换配色」。
- 字体许可写进 `theme.json` 并由测试校验（复用现有 `license` 字段惯例）。

### 4.4 导出档位（按模式决定，用户已定默认＝保真优先）

模式 A 不存在这个取舍——它天然既好看又原生可编辑。取舍只发生在模式 B：

| | 模式 A：克隆页填文案 | 模式 B 保真档（该模式默认） | 模式 B 原生档 |
|---|---|---|---|
| 做法 | 替换模板页里的样例文案 | 整页底图 + 可编辑文本框叠加 | IR → 原生 shape/表格/图表 |
| 视觉 | = 模板设计水平 | 与预览 100% 一致 | 主题的简化还原 |
| 可编辑 | **全部可改** | 文字可改，形状不可改 | 全部可改 |
| 适用 | 首选 | 模板缺版式且要观感 | 模板缺版式且要交给别人改版式 |

界面规则：模式 A 不暴露档位选择（无取舍可选）。落到模式 B 时才提示，并直说取舍。
禁止默认给保真档却宣称「完全可编辑」——这是本 PRD 的诚实红线之一。

### 4.5 容量与视觉检查

模式 A 的第一道检查不需要截图：每个槽有字数预算（= 样例文案字数）和字号下限。超预算按 4.1a 第 5 步处理，并在质检里记下「哪一页哪一个槽被缩了字 / 被拆成续页」。这一步就能抓住大部分串版。

截图回流（溢出、遮挡、对比度）留给模式 B，以及模式 A 里用户点名「这页字出框了」的复检。`visualScore` 在模式 A 上先定义成可计算的三项：槽超预算次数、缺字体次数、纯图页被当成可编辑页的次数。`designerReviewed` 仍只能由人置位。

两个独立指标，不许互相冒充：

- `visualScore`：自动规则分（本期要变成真实数值）
- `designerReviewed`：人工复核标记（自动分永远不能把它置为非 0）

### 4.6 交互简化：20+ 字段 → 3 步

1. **一句话目标**（+ 可选粘贴材料：周报、纪要、表格）
2. **选一套模板**：只显示 `D:\PPT模板库` 当前分类。卡片用封面缩略图，副标题写「可改字的页 / 纯图页」。默认分类是「工作总结」
3. **大纲确认**：每一节绑到模板里的某一页，可换页、删页、调顺序。不再逐页填「页目的 / 页结论」

然后**生成按钮放回工作台**（修掉 1.7 那条「工作台不能生成」）。

其余下沉：

- 受众 / 用途 / 密级 → 折叠进「更多设定」，默认不展开
- 品牌登记（编号 / 来源 / 许可 / 64 位摘要）→ 移到设置页，普通用户永不接触
- 四条工程免责声明 → 从主界面移除，改为文档与 hover 说明
- 「工程变体，非设计师已检 36」→ 不在用户界面出现

### 4.7 路径收敛

`pptx.gen` 不再自己写文件，改为转调 `office.generate`（`internal/app/office_generate_adapt.go` 已有 shape 适配代码）。
聊天与工作台共用同一条画质。同时重新评估九步流水线闸门（`pptGenBlocked`）：闸门的价值在内容完整性，不该让用户为丑的输出多等几轮。

### 4.8 素材层

模式 A 的装饰、图标、背景已经在模板页里，**不要再往设计师页上自动贴一套 Lucide**。那会把两套视觉叠在一起。

- 图表：模板页里已有图表框时，只换数据；模板页没有图表槽时，不新画一套图盖上去
- 配图：沿用模板自带图片。联网图库仍是 P2，且默认关
- 字体：生成时引用模板已经引用的字体。产品安装包不捆绑思源 / 普惠体；本机有就用，没有就按 4.1a 第 7 步报告
- Lucide、ECharts、自绘背景只属于模式 B，模式 B 未启用时用户看不到它们

---

## 5. 明确不做（本期）

- 不重写 Office Studio 的版本链、QA、导出、成套导出、存储体系
- 不把任意 PPTX 反编译成可重排的母版。做的是克隆已有页、替换样例文案。几何重排留给模式 B
- 不自动擦除图片水印，不改形状内部的觅知 ID 名字
- 不在纯图页上叠新文本框，假装那页可以改字
- 不做参考图复刻
- 不引入云沙箱、云端无头浏览器
- 不为了「完全自由排版」在 P0 同时做 HTML 主题。模式 B 等模式 A 的一页填充在 PowerPoint 里看过再开

---

## 6. 分期

### 6.1 P0：用真实模板填一页（不做产品化）

模板已经在 `D:\PPT模板库\工作总结`，普查已完成（3.3）。P0 只回答一个问题：替换样例文案之后，PowerPoint 里还是不是那一页设计。

固定同一段经营汇报（封面一句话 + 六个要点），做两页成品，不接模型、不接 UI：

| 页 | 来源 | 要替换的原文 | 证明什么 |
|---|---|---|---|
| 散装文本框页 | `工作总结.pptx` 第 4 页 | 「添加标题」「此处添加详细文本描述，建议与标题相关」 | 没有占位符的页也能填，装饰还在 |
| 占位符 + 外部正文页 | `76f50305…pptx` 第 8 页 | 「任务一/二/三」及后面的说明句；占位符「完成情况」 | 两种形状走同一套替换 |

对照物是已经生成的当前管线基线（5 页、12 KiB、零占位符）。盲评只看三样：版面是不是模板原样、六个要点是否都在且没有出框、打开后改一个字是否还在原来的格子里。

**出口**

- 两页都过 → 按 6.2 做库和三步向导，模式 B 不动
- 只有占位符页过、散装页失败 → 停。用户真正会选的 4 套都是散装页，只做占位符没有产品价值
- 字出框或装饰丢了 → 先修替换范围，不进入 UI

### 6.2 P1：模板库成为生成入口

- 索引 `D:\PPT模板库`，分类 = 子文件夹，首期「工作总结」
- 槽识别、克隆页、替换 `<a:t>`、字数预算、缺字体报告
- 三步向导；生成按钮回到工作台；品牌登记与工程声明离开主界面
- 聊天里的 `pptx.gen` 改走同一条填充，不再自己写 `slideLayout1`
- 模式 B（HTML 主题、保真底图）不在这一期

### 6.3 P2：追平差异化能力

- 原生档完善（表格、图表、图标的原生映射）
- 用户上传 PPTX 母版还原
- 参考图复刻
- 在线版式微调（圈选改文案 / 换图 / 改图表已有，补版式）
- 引用核验（现有 facts / evidenceRefs 已有骨架）
- 联网配图（带开关）

---

## 7. 钱花在哪

用户材料里的付费清单落到「本地桌面产品」这个形态，可以省掉相当一部分。

**必须花**

| 项 | 说明 |
|---|---|
| 评测集 + judge token | 持续性支出，用户材料也提醒了这是隐性大头 |

**设计人力不再单列**：观感来自 `D:\PPT模板库` 里已有的 19 套（以及以后放进去的文件夹），不外包、不自产 HTML 主题。本期工程量是索引、槽识别和文本替换。

**不需要花（本地形态的优势）**

| 项 | 为什么不需要 |
|---|---|
| 云沙箱（E2B / Daytona / Firecracker） | 已是本机执行 + Job Object 隔离 |
| 云端无头浏览器（Browserbase / Steel） | 本机已有 WebView2，且已有渲染工作器 |
| 商用字体授权 | 用思源 / Noto（OFL）规避 |
| 图标 / 图表库 | Lucide + ECharts 免费 |
| 搜索抓取 API | 已有 `web.search` / `web.fetch` |

**可选**：高级图库（摄图 / Getty），仅在客户要求商业级配图时采购。

---

## 8. 风险与红线

| 风险 | 对策 |
|---|---|
| **只填占位符，成品还是模板里的样例字** | 实测 4 套改名模板占位符为 0，齐全的套里正文也在占位符外。槽按文案识别，P0 用第 4 页和第 8 页各证一次 |
| **样例文案词典误伤真实句子** | 只把整段等于或以前缀匹配「添加标题 / 此处添加 / 在这里输入 / 关键词：」的形状当空槽；已经写成具体业务句的形状（「市场份额稳步提升」）标成「待确认」，不静默覆盖。P0 的第 4 页全是空槽，第 8 页用来看待确认是否过多 |
| **纯图页被填了等于没填** | 索引标明 `text-shapes=0`，界面写「文字在图片里」。禁止往上叠文本框 |
| **字数超出格子** | 预算 = 样例字数；超了先降该形状字号，再克隆同一页。禁止把长文塞进 4 字标题框 |
| **PingFang 等字体本机没有** | 生成前对照本机字体，质检点名，不静默改引用 |
| **同一模板存了两份** | 结构哈希折叠，避免分类里两张一样的卡片 |
| **素材误入仓库或安装包** | 库根目录在仓库外；测试断言 release 清单不含这些 pptx |
| 底图导出让形状不可编辑（模式 B） | 双档 + 界面直说取舍；禁止默认保真档却宣称「完全可编辑」 |
| 主题包退化成 slot 白名单（1.1 复发） | 验收测试：任意两套主题同名 slot 的 HTML 结构必须不同 |
| 视觉分再次自我豁免（1.2 复发） | 验收测试：视觉回流必须真实降低溢出 / 对比度违规计数，否则判未达标 |
| 备选后端再次停在环境变量探针（1.3 复发） | 渲染后端必须在产品默认路径上，不靠 `os.Getenv` 开启 |
| 重写现有体系导致回归 | 版本链 / QA / 导出 / patch 一律复用，改动限定在设计源层 |
| 字体许可法律风险 | `theme.json` 声明许可，测试校验，仅允许可嵌入可商用字体 |
| 中文排版串版 | 保真档天然绕开字体度量差异；原生档需单独做中文度量校验 |

---

## 9. 决策记录（2026-09-22 已确认）

| 议题 | 决定 | 影响 |
|---|---|---|
| 是否先跑 P0 | **先跑 P0**，验证方向再投入 | 6.1 生效，含新增 E 组 |
| 默认导出档 | **保真档**（视觉与预览一致，文字可改、形状不可改） | 仅适用模式 B；模式 A 天然原生可编辑，不受此选择约束 |
| 主题来源 | **不外包，自行获取**；用户提供 51miz 个人版终身会员作为模板与素材来源 | 消除最大成本项，模式 A 升为主路径，见 3.0 / 3.2 / 7 |
| 配图策略 | **全离线内嵌素材**，联网图库排 P2 且带开关 | 4.8 生效 |
| 授权口径 | 个人自用、不商用；素材不入仓库、不随安装包分发 | 3.2 边界 |

| 模板库位置 | **`D:\PPT模板库`**，子文件夹即分类；首期只用 **`工作总结`**（19 套已普查） | 3.2、3.3、4.1a |
| 观感方向 | **克制商务风** | 从「工作总结」里选页时优先留白大、色少的页；模式 B 未启用，不另写 HTML 主题 |
| 填充单位 | **样例文本框，不是占位符** | 3.3 推翻了「只填 ph」；4.1a 是修订后的主路径 |

### 仍待确认

1. **英文对照行**：默认由中文标题生成一句短英文，还是保留模板里的英文不改？建议默认生成，设置里可关。
2. **纯图页**：默认可以当章节隔页插入（并标明不能改字），还是直接从候选里隐藏？建议插入但标明。
3. **缺字体**：默认只报告，还是提供「这一套改用微软雅黑」？建议默认只报告。
