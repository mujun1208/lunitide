{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://lunitide.local/schema/bridge/v1/tools.hooksPolicy.get.schema.json",
  "title": "tools.hooksPolicy.get payload",
  "x-method": "tools.hooksPolicy.get",
  "x-owner": "engine",
  "x-enabled": true,
  "type": "object",
  "additionalProperties": false,
  "properties": {},
  "x-result": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "hooks": {
        "type": "array",
        "maxItems": 64,
        "items": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "id": { "type": "string", "minLength": 1, "maxLength": 64 },
            "events": {
              "type": "array",
              "minItems": 1,
              "maxItems": 2,
              "items": { "enum": ["beforeToolCall", "afterToolCall"] }
            },
            "tools": {
              "type": "array",
              "minItems": 1,
              "maxItems": 14,
              "items": {
                "enum": [
                  "workspace.list",
                  "workspace.read",
                  "workspace.write",
                  "workspace.search",
                  "workspace.edit",
                  "todo.write",
                  "command.run",
                  "web.fetch",
                  "web.search",
                  "excel.gen",
                  "excel.parse",
                  "docx.gen",
                  "pptx.gen",
                  "pdf.gen"
                ]
              }
            },
            "decision": { "enum": ["block", "requireApproval", "allow"] },
            "message": { "type": "string", "maxLength": 256 }
          },
          "required": ["id", "events", "tools", "decision"]
        }
      }
    },
    "required": ["hooks"]
  },
  "x-examples": {
    "positive": [
      {}
    ],
    "negative": [
      {
        "unexpected": true
      }
    ]
  }
}
