{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://lunitide.local/schema/bridge/v1/skill.catalog.list.schema.json",
  "title": "skill.catalog.list payload",
  "x-method": "skill.catalog.list",
  "x-owner": "engine",
  "x-enabled": true,
  "type": "object",
  "additionalProperties": false,
  "properties": {},
  "x-result": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "items": {
        "type": "array",
        "minItems": 0,
        "maxItems": 64,
        "items": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "id": { "type": "string", "minLength": 1, "maxLength": 64 },
            "name": { "type": "string", "minLength": 1, "maxLength": 128 },
            "displayName": { "type": "string", "minLength": 1, "maxLength": 200 },
            "description": { "type": "string", "maxLength": 4096 },
            "category": { "type": "string", "minLength": 1, "maxLength": 64 },
            "version": { "type": "string", "minLength": 1, "maxLength": 32 },
            "permissions": {
              "type": "array",
              "minItems": 1,
              "maxItems": 6,
              "items": { "enum": ["read_only", "read_write", "network", "file_system", "shell", "admin"] }
            },
            "installed": { "type": "boolean" }
          },
          "required": ["id", "name", "displayName", "description", "category", "version", "permissions", "installed"]
        }
      }
    },
    "required": ["items"]
  },
  "x-examples": {
    "positive": [{}],
    "negative": [{ "unexpected": true }]
  }
}
