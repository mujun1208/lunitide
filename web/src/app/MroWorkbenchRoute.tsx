import React from'react'
import{createMutationAttempt,datasourceBridge,type ExpertBridge,type MroBridge,type ProjectBridge,type SessionBridge}from'../bridge/client'
import{openMroChat}from'../mro/MroAskButton'
import{MroWorkbenchPage}from'../mro/MroWorkbenchPage'
import type{WorkbenchRail}from'../expert/expertIds'
import{ensurePersonalProject}from'./appHelpers'
import type{LaunchTarget}from'./appTypes'

export function MroWorkbenchRoute({mro,experts,projects,sessions,mroEnabled,mroExpertId,opsExpertIds,mroInitialRail,setDraftSessionIds,setTarget}:{mro:MroBridge;experts:ExpertBridge;projects:ProjectBridge;sessions:SessionBridge;mroEnabled:boolean;mroExpertId:string;opsExpertIds:Record<string,string>;mroInitialRail:WorkbenchRail;setDraftSessionIds:React.Dispatch<React.SetStateAction<Set<string>>>;setTarget:(target:LaunchTarget|undefined)=>void}):React.JSX.Element{
 return <MroWorkbenchPage enabled={mroEnabled} mroExpertId={mroExpertId} opsExpertIds={opsExpertIds} initialRail={mroInitialRail} aircraftList={()=>mro.aircraftList({}).then(r=>({items:r.items.map(item=>({aircraftId:item.aircraftId,tailNo:item.tailNo,msn:item.msn??'',model:item.model,config:item.config??''}))}))} manualList={()=>mro.manualList({}).then(r=>({items:r.items.map(item=>({manualId:item.manualId,title:item.title??'',docType:item.docType,revision:item.revision,status:item.status,ata:item.ata??'',sectionCount:item.sectionCount}))}))} onUpsertAircraft={async input=>{const row=await mro.aircraftUpsert(input,{attempt:createMutationAttempt('mro.aircraft.upsert',input)});return{aircraftId:row.aircraftId,tailNo:row.tailNo,msn:row.msn??'',model:row.model,config:row.config??''}}} verifiedConnections={async()=>{const listed=await datasourceBridge.list({}).catch(()=>({items:[]}));return listed.items.filter(item=>item.readonlyVerified&&item.state==='active').map(item=>({id:item.id,name:item.name,kind:item.kind}))}} onBindStock={async input=>{const payload={ownerType:'mro' as const,ownerId:'workbench',connectionId:input.connectionId,purpose:'stock' as const,tableMap:input.tableMap};await datasourceBridge.bind(payload,{attempt:createMutationAttempt('datasource.bind',payload)})}} onRegisterManual={async input=>{const payload={title:input.title,docType:input.docType as 'AMM',revision:input.revision,status:input.status as 'controlled'|'uncontrolled'|'superseded',ata:input.ata,documents:input.documents};const row=await mro.manualRegister(payload,{attempt:createMutationAttempt('mro.manual.register',payload)});return{manualId:row.manualId,title:row.title??'',docType:row.docType,revision:row.revision,status:row.status,ata:row.ata??'',sectionCount:row.sectionCount}}} onIngestManual={input=>experts.knowledgeIngest!({expertId:input.expertId,path:input.path,sourceLocator:input.sourceLocator,...(input.mediaType?{mediaType:input.mediaType}:{})})} onBuildChecklist={input=>mro.checklistBuild(input)} auditList={()=>mro.auditList({})} dueList={()=>mro.dueList?.({})??Promise.resolve({items:[]})} toolList={()=>mro.toolList?.({})??Promise.resolve({items:[]})} lotList={()=>mro.lotTrace?.({})??Promise.resolve({items:[]})} kitList={()=>mro.kitStaging?.({})??Promise.resolve({items:[]})} partsList={()=>mro.partsStockList?.({})??Promise.resolve({items:[],alternates:[]})} planList={()=>mro.planList?.({})??Promise.resolve({items:[]})} todoList={()=>mro.opsTodoList?.({})??Promise.resolve({items:[]})} constraintList={()=>mro.planConstraintCheck?.({})??Promise.resolve({violations:[]})} onCheckoutTool={async id=>{const payload={toolId:id,holder:'workbench'};if(!mro.toolCheckout)return{ok:false,reason:'unavailable'};return mro.toolCheckout(payload,{attempt:createMutationAttempt('mro.tool.checkout',payload)})}} onPublishSchedule={async id=>{const payload={packageId:id};if(!mro.planPublish)return{todos:[]};return mro.planPublish(payload,{attempt:createMutationAttempt('mro.plan.publish',payload)})}} onBulletinChain={async lotId=>{if(!mro.lotTrace)return{tails:[],note:''};const listed=await mro.lotTrace({lotId});const hit=listed.items[0];return{tails:hit?.tails??[],note:hit?.lotNo??''}}} componentListFn={()=>mro.componentList?.({})??Promise.resolve({items:[]})} pirepListFn={()=>mro.pirepList?.({})??Promise.resolve({items:[]})} aogListFn={()=>mro.aogList?.({})??Promise.resolve({items:[]})} poListFn={()=>mro.poList?.({})??Promise.resolve({items:[]})} triggerListFn={()=>mro.triggerList?.({})??Promise.resolve({items:[]})} intervalListFn={()=>mro.intervalList?.({})??Promise.resolve({items:[]})} onAddTool={async input=>{if(!mro.toolUpsert)return{ok:false};return mro.toolUpsert(input,{attempt:createMutationAttempt('mro.tool.upsert',input)})}} onReturnTool={async id=>{const p={toolId:id};if(!mro.toolReturn)return{ok:false};return mro.toolReturn(p,{attempt:createMutationAttempt('mro.tool.return',p)})}} onAddDue={async input=>{if(!mro.dueUpsert)return{ok:false};return mro.dueUpsert(input,{attempt:createMutationAttempt('mro.due.upsert',input)})}} onRecordUtil={async input=>{if(!mro.utilRecord)return{items:[]};return mro.utilRecord(input,{attempt:createMutationAttempt('mro.util.record',input)})}} onAddLot={async input=>{if(!mro.lotUpsert)return{ok:false};return mro.lotUpsert(input,{attempt:createMutationAttempt('mro.lot.upsert',input)})}} onRecordUse={async input=>{if(!mro.lotUse)return{ok:false};return mro.lotUse(input,{attempt:createMutationAttempt('mro.lot.use',input)})}} onDefineKit={async input=>{if(!mro.kitUpsert)return{ok:false};return mro.kitUpsert(input,{attempt:createMutationAttempt('mro.kit.upsert',input)})}} onAddStock={async input=>{if(!mro.partsStockUpsert)return{ok:false};return mro.partsStockUpsert(input,{attempt:createMutationAttempt('mro.parts.stock.upsert',input)})}} onAddAlternate={async input=>{if(!mro.alternateUpsert)return{ok:false};return mro.alternateUpsert(input,{attempt:createMutationAttempt('mro.alternate.upsert',input)})}} onBuildWp={async input=>{if(!mro.workpackageBuild)return{ok:false};return mro.workpackageBuild(input,{attempt:createMutationAttempt('mro.workpackage.build',input)})}} onAddInterval={async input=>{if(!mro.intervalUpsert)return{ok:false};return mro.intervalUpsert(input,{attempt:createMutationAttempt('mro.interval.upsert',input)})}} onProposeInterval={async input=>{if(!mro.intervalPropose)return{ok:false};return mro.intervalPropose(input,{attempt:createMutationAttempt('mro.interval.propose',input)})}} onAddSchedule={async input=>{if(!mro.scheduleUpsert)return{ok:false};return mro.scheduleUpsert(input,{attempt:createMutationAttempt('mro.schedule.upsert',input)})}} onSetCapacity={async input=>{if(!mro.capacityUpsert)return{ok:false};return mro.capacityUpsert(input,{attempt:createMutationAttempt('mro.capacity.upsert',input)})}} onAddComponent={async input=>{if(!mro.componentUpsert)return{ok:false};return mro.componentUpsert(input,{attempt:createMutationAttempt('mro.component.upsert',input)})}} onLifeEvent={async input=>{if(!mro.lifeEvent)return{ok:false};return mro.lifeEvent(input,{attempt:createMutationAttempt('mro.life.event',input)})}} onDraftPirep={async input=>{if(!mro.pirepDraft)return{ok:false};return mro.pirepDraft(input,{attempt:createMutationAttempt('mro.pirep.draft',input)})}} onIntakeAog={async input=>{if(!mro.aogIntake)return{ok:false};return mro.aogIntake(input,{attempt:createMutationAttempt('mro.aog.intake',input)})}} onDraftPo={async input=>{if(!mro.poDraft)return{ok:false};return mro.poDraft(input,{attempt:createMutationAttempt('mro.po.draft',input)})}} onIssueChem={async input=>{if(!mro.chemIssue)return{ok:false};return mro.chemIssue(input,{attempt:createMutationAttempt('mro.chem.issue',input)})}} onAddPartsTodo={async input=>{if(!mro.opsTodoAdd)return{ok:false};return mro.opsTodoAdd(input,{attempt:createMutationAttempt('mro.ops.todo.add',input)})}} onConfirmPirep={async input=>{if(!mro.pirepConfirm)return{ok:false};return mro.pirepConfirm(input,{attempt:createMutationAttempt('mro.pirep.confirm',input)})}} onConfirmAog={async input=>{if(!mro.aogConfirm)return{ok:false};return mro.aogConfirm(input,{attempt:createMutationAttempt('mro.aog.confirm',input)})}} onConfirmPo={async input=>{if(!mro.poConfirm)return{ok:false};return mro.poConfirm(input,{attempt:createMutationAttempt('mro.po.confirm',input)})}} openChat={input=>openMroChat({...input,ensureProject:ensurePersonalProject,projects,sessions,experts})} onAskOpened={opened=>{setDraftSessionIds(values=>new Set(values).add(opened.session.id));setTarget({project:opened.project,session:opened.session,personal:true,...(opened.prompt?{prompt:opened.prompt}:{})})}}/>
}